[project]
name = "{{cookiecutter.project_slug}}"
version = "0.5.0"
description = "CLI-first experiment project scaffold powered by expctl public beta"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "pandas",
  "pyyaml",
  "scikit-learn"
]

[tool.pytest.ini_options]
pythonpath = ["."]
