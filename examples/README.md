# Examples

Official beta examples:

- `examples/binary_classification`
- `examples/regression`
- `examples/grouped_classification`
- `examples/time_series_regression`

Each example is a self-contained project root. Run commands from inside the
example directory or pass `--repo-root` explicitly.
